import styled from "styled-components";
import {overlayOnHover} from "../GeneralComponents/StyledComponents";

export const Wrapper = styled.div`
  display: flex;
`

export const Button = styled.div`
  background-color: #fff;
  padding: 8px 16px;
  cursor: pointer;
  user-select: none;
`